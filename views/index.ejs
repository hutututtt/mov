<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <link href="./css/style.css" rel="stylesheet">
    <style>
        /* 内联基础样式，防止CSS加载失败 */
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            background: #0c0c0c;
            color: #ffffff;
            margin: 0;
            padding: 0;
        }
        .loading-fallback {
            display: none;
            text-align: center;
            padding: 50px;
            color: #007AFF;
        }
    </style>
</head>
<body>
    <!-- 深色系导航栏 -->
    <nav class="apple-navbar">
        <div class="nav-container">
            <div class="nav-logo">
                <div class="logo-icon">
                    <i class="fas fa-play-circle"></i>
                </div>
                <span class="logo-text">Cinema</span>
            </div>
            
            <div class="nav-menu">
                <a href="#" class="nav-link active" data-category="hot">
                    <span>首页</span>
                </a>
                <a href="#" class="nav-link" data-category="action">
                    <span>电影</span>
                </a>
                <a href="#" class="nav-link" data-category="comedy">
                    <span>电视剧</span>
                </a>
                <a href="#" class="nav-link" data-category="drama">
                    <span>综艺</span>
                </a>
            </div>
            
            <div class="nav-actions">
                <div class="search-container">
                    <div class="search-input-wrapper">
                        <i class="fas fa-search search-icon"></i>
                        <input type="text" id="searchInput" placeholder="搜索电影、电视剧..." autocomplete="off">
                        <button id="searchBtn" class="search-btn">
                            <i class="fas fa-arrow-right"></i>
                        </button>
                    </div>
                    <div id="searchSuggestions" class="search-suggestions-dropdown">
                        <!-- 搜索建议将通过JavaScript动态生成 -->
                    </div>
                </div>
            </div>
        </div>
    </nav>


    <!-- 主要内容区域 -->
    <main class="main-content">
        <!-- 加载指示器 -->
        <div id="loading" class="apple-loading">
            <div class="loading-spinner"></div>
            <p class="loading-text">加载中...</p>
        </div>

        <!-- 电影列表 -->
        <div id="movieList" class="apple-movie-grid">
            <!-- 电影卡片将通过JavaScript动态生成 -->
        </div>

        <!-- 分页 -->
        <div id="pagination" class="apple-pagination">
            <!-- 分页按钮将通过JavaScript动态生成 -->
        </div>
    </main>

    <!-- 电影详情模态框 -->
    <div id="movieModal" class="modal">
        <div class="modal-content">
            <span class="close">&times;</span>
            <div id="movieDetail" class="movie-detail">
                <!-- 电影详情将通过JavaScript动态生成 -->
            </div>
        </div>
    </div>

    <!-- 播放器模态框 -->
    <div id="playerModal" class="modal">
        <div class="modal-content player-modal">
            <span class="close">&times;</span>
            <div id="videoPlayer">
                <!-- 视频播放器将通过JavaScript动态生成 -->
            </div>
        </div>
    </div>

    <!-- 错误提示 -->
    <!-- <div id="errorToast" class="error-toast">
        <p id="errorMessage"></p>
    </div> -->

    <!-- 脚本 -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/hls.js/1.4.12/hls.min.js"></script>
    <script src="./js/app.js"></script>
</body>
</html>
